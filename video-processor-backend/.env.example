# Frontend environment variables (Vite)
# Copy this to .env.local for local development
# Set VITE_API_URL in your Firebase Hosting / Vite build environment for production

# Override the API server URL (optional — falls back to localhost in dev, Render URL in prod)
# VITE_API_URL=https://your-render-service.onrender.com/api
